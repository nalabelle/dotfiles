# Template merge configuration
# Use "ours" strategy for files that should keep local versions during template merges

README.md merge=ours
.gitignore merge=ours

# Treat JSONC files as JSON for diff purposes
*.jsonc diff=json
